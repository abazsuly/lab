apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod
  labels:
    app: myapp
    type: front-end
spec:
  containers:
    - image: nginx
      name: nginx
      resources:
        requests:
          memory: "0.5Gi"
          cpu: 0.5
        limits:
          memory: "0.5Gi"
          cpu: 0.5
